query BorjomiSearch {
  products(first: 20, query: "ბორჯომი") {
    nodes { id handle title tags }
  }
  collections(first: 10, query: "ბორჯომი") {
    nodes { id handle title productsCount { count } }
  }
}
